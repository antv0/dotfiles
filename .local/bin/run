#!/bin/sh

filename=$1

case "$filename" in
    *.cpp)      g++ -o "${filename%.*}" $filename && ./"${filename%.*}" ;;
    *.c)        gcc -o "${filename%.*}" $filename && ./"${filename%.*}" ;;
    *.py)       python $filename ;;
    *.hs)       runghc $filename ;;
	*)          $filename ;;
esac
