#!/bin/sh

filename=$1

case "$filename" in
    *.cpp)      compile -O3 -fopenmp $filename && ./"${filename%.*}";;
    *.c)        compile $filename && ./"${filename%.*}" ;;
    *.py)       python $filename ;;
    *.hs)       runghc $filename ;;
	*)          $filename ;;
esac
